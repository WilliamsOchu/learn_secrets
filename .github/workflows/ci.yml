name: CI

on:
    push:
        branches:
            - master

jobs:
    display:
        runs-on: ubuntu-latest



        steps:
            - name: Display custom variables
              run: |
                echo "AFRICA"
                python3 /testing_secrets.py




